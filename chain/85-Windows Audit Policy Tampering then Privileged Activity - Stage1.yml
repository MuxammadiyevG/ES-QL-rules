rule_id: 088454aa-9c78-462a-b3d1-e8bdda0d1dee
name: "Chain: Windows Audit Policy Tampering then Privileged Activity — Stage 1: Audit Policy Source Manipulation"
description: |
  Windows Audit Policy Tampering then Privileged Activity zanjiri — 1-bosqich (2 dan).
  Audit Policy Source Manipulation.
type: esql
enabled: true
params: "{}"
schedule_interval: 5m
index:
  - logs-winlog*
query_language: esql
query: |
  FROM logs-winlog* | WHERE @timestamp >= NOW() - 15 minutes | WHERE winlog.channel == "Security" AND event.code IN ("4719", "4904", "4905") | EVAL host_name = winlog.computer_name, user_name = winlog.event_data.SubjectUserName, event_action = event.code | KEEP @timestamp, event.code, event_action, host_name, user_name, winlog.channel | LIMIT 500
tags:
  - attack.defense-evasion
  - audit-policy-tampering
  - chain
  - correlation
  - windows
  - winlog
severity: medium
risk_score: 47
max_signals: 100
correlation:
  enabled: true
  same_fields:
    - host_name
    - winlog.channel
  timeframe_sec: 3600
  dedup_fields:
    - host_name
    - event.code
chain:
  enabled: true
  stage: 1
  timeframe_sec: 3600
  correlate_by:
    - host_name
mitre_attack:
  TA0005:
    T1562: ["T1562.002"]
