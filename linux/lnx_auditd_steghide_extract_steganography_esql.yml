rule_id: bab90c52-e483-44f8-8d92-5207322e521d
name_uz: "Steghide bilan steganografiyani chiqarish"
name_ru: "Извлечение данных стеганографией Steghide"
name_en: "Steghide Extract Steganography"
description_uz: |
  Rasmlardan ma'lumot chiqarish uchun steghide'dan foydalanishni aniqlaydi.
description_ru: |
  Обнаруживает использование steghide для извлечения данных из изображений.
description_en: |
  Detects usage of steghide to extract data from images.
type: esql
params: '{}'
schedule_interval: 10m
index:
  - logs-updive.audit-*
query_language: esql
query: |
  FROM logs-updive.audit-*
  | WHERE @timestamp >= NOW() - 10 minutes
  | WHERE event.action == "executed"
  | WHERE process.name == "steghide" AND TO_LOWER(COALESCE(MV_CONCAT(process.args, " "), "")) RLIKE ".*extract.*"
  | STATS
      event_count = COUNT(*),
      users = VALUES(user.name),
      processes = VALUES(process.name),
      command_lines = VALUES(process.title),
      hosts = VALUES(host.name)
    BY agent_id, host.name, user.name, bucket = DATE_TRUNC(10 minutes, @timestamp)
  | KEEP agent_id, bucket, host.name, user.name, event_count, users, processes, command_lines, hosts
  | SORT event_count DESC
enabled: false
tags:
  - attack.defense-evasion
  - attack.t1027.003
severity: medium
risk_score: 60
nist: ["SI-4", "AU-12"]
pci_dss: ["10.2"]
gdpr: ["32.1.b"]
mitre_attack:
  TA0005:
    T1027: ["T1027.003"]
