rule_id: 558f45bf-2f3a-43a4-ad8d-09cac495fd46
name_uz: "Agent Info: Foydalanuvchi sessiyasi ochildi (muvaffaqiyatli SSH login)"
name_ru: "Agent Info: Открыта сессия пользователя (успешный вход по SSH)"
name_en: "Agent Info: User Session Opened (SSH Login Success)"
description_uz: |
  Tizimga SSH orqali muvaffaqiyatli kirgan foydalanuvchilar haqida ma'lumot beradi.
  Bu qoida threat hunting va sessiya monitoring uchun ishlatiladi.
description_ru: |
  Предоставляет информацию о пользователях, успешно вошедших в систему по SSH.
  Это правило используется для threat hunting и мониторинга сессий.
description_en: |
  Provides information about users who successfully logged in to the system via SSH.
  This rule is used for threat hunting and session monitoring.
type: esql
params: '{}'
schedule_interval: 5m
index:
  - logs-updive.audit-*
query_language: esql
query: |
  FROM logs-updive.audit-*
  | WHERE @timestamp >= NOW() - 300 seconds
  | WHERE auditd.message_type == "user_login"
  | WHERE auditd.result == "success"
  | WHERE network.direction == "ingress"
  | STATS
      event_count  = COUNT(*),
      host_names   = VALUES(host.hostname),
      users        = VALUES(user.audit.name),
      src_ips      = VALUES(source.ip),
      terminals    = VALUES(auditd.data.terminal),
      session_ids  = VALUES(auditd.session),
      last_seen    = MAX(@timestamp),
      first_seen   = MIN(@timestamp)
    BY agent_id, host.hostname, user.audit.name, source.ip
  | WHERE event_count >= 1
  | KEEP
      agent_id, host.hostname, user.audit.name, source.ip,
      event_count, host_names, users, src_ips,
      terminals, session_ids, last_seen, first_seen
  | SORT last_seen DESC
enabled: true
tags:
  - detection.informational
  - visibility.authentication
severity: low
risk_score: 10
references: []
nist: ["AU-12", "AC-17"]
pci_dss: ["10.2.1"]
gdpr: ["32.1.b"]
mitre_attack: []