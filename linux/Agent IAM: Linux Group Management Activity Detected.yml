rule_id: f571b8ef-8245-41aa-b34f-a21407d59c7a
name_uz: "Agent IAM: Linux guruhlarini boshqarish faoliyati aniqlandi"
name_ru: "Agent IAM: Обнаружена активность управления группами Linux"
name_en: "Agent IAM: Linux Group Management Activity Detected"
description_uz: |
  Ushbu qoida Persistence va Privilege Escalation texnikalariga bog'liq bo'lib, guruh yaratish yoki o'zgartirish hodisalarini aniqlaydi.
  Hujumchi o'zini maxsus guruhga (masalan, docker, sudo, wheel) qo'shib, huquqlarini oshirishi mumkin.
  Qonuniy holatlar: tizim administratori tomonidan rejalashtirilgan IAM o'zgarishlari, deployment avtomatsiyasi.
  SOC triage bosqichlari:
  - `auditd.data.op` qiymatini tekshiring: add-group, add-shadow-group, delete-group.
  - O'zgartirilgan guruh ID (`auditd.data.id`) va nomini aniqlang.
  - O'zgarishni amalga oshirgan process (`auditd.summary.how`) ni tekshiring.
  - Terminal (`auditd.data.terminal`) interaktiv yoki avtomatik ekanligini aniqlang.
  - Noqonuniy bo'lsa: guruh o'zgarishlarini qaytaring, akkauntni tekshiring.
description_ru: |
  Это правило связано с техниками Persistence и Privilege Escalation и выявляет события создания или изменения групп.
  Злоумышленник может добавить себя в особую группу (например, docker, sudo, wheel) и повысить свои привилегии.
  Легитимные случаи: плановые изменения IAM системным администратором, автоматизация развёртывания.
  Шаги SOC-триажа:
  - Проверьте значение `auditd.data.op`: add-group, add-shadow-group, delete-group.
  - Определите ID (`auditd.data.id`) и имя изменённой группы.
  - Проверьте процесс (`auditd.summary.how`), выполнивший изменение.
  - Определите, является ли терминал (`auditd.data.terminal`) интерактивным или автоматическим.
  - Если нелегитимно: откатите изменения групп, проверьте учётную запись.
description_en: |
  This rule is related to Persistence and Privilege Escalation techniques and detects group creation or modification events.
  An attacker may add themselves to a special group (for example, docker, sudo, wheel) and escalate their privileges.
  Legitimate cases: planned IAM changes by a system administrator, deployment automation.
  SOC triage steps:
  - Check the `auditd.data.op` value: add-group, add-shadow-group, delete-group.
  - Identify the ID (`auditd.data.id`) and the name of the modified group.
  - Check the process (`auditd.summary.how`) that made the change.
  - Determine whether the terminal (`auditd.data.terminal`) is interactive or automated.
  - If illegitimate: revert the group changes, review the account.
type: esql
params: '{}'
schedule_interval: 5m
index:
  - logs-updive.audit-*
query_language: esql
query: |
  FROM logs-updive.audit-*
  | WHERE @timestamp >= NOW() - 300 seconds
  | WHERE auditd.message_type IN ("add_group", "grp_mgmt", "del_group")
  | EVAL host_name    = host.hostname
  | EVAL actor        = COALESCE(user.effective.name, auditd.summary.actor.secondary)
  | EVAL group_id     = auditd.data.id
  | EVAL operation    = auditd.data.op
  | EVAL how          = auditd.summary.how
  | EVAL terminal     = auditd.data.terminal
  | EVAL msg_type     = auditd.message_type
  | WHERE group_id IS NOT NULL
  | STATS
      event_count  = COUNT(*),
      host_names   = VALUES(host.hostname),
      actors       = VALUES(user.effective.name),
      group_ids    = VALUES(auditd.data.id),
      operations   = VALUES(auditd.data.op),
      executables  = VALUES(auditd.summary.how),
      msg_types    = VALUES(auditd.message_type),
      terminals    = VALUES(auditd.data.terminal),
      last_seen    = MAX(@timestamp),
      first_seen   = MIN(@timestamp)
    BY agent_id, host.hostname, auditd.data.op, user.effective.name
  | WHERE event_count >= 1
  | EVAL
      host_name  = host.hostname,
      operation  = auditd.data.op,
      actor      = user.effective.name
  | KEEP
      agent_id, host_name, operation, actor, event_count,
      host_names, actors, group_ids, operations,
      executables, msg_types, terminals, last_seen, first_seen
  | SORT event_count DESC
enabled: true
tags:
  - attack.persistence
  - attack.privilege-escalation
  - attack.t1136.001
  - attack.t1078
  - detection.threat-hunting
severity: medium
risk_score: 55
references:
  - "https://attack.mitre.org/techniques/T1136/001/"
  - "https://attack.mitre.org/techniques/T1078/"
nist: ["AC-2", "AC-6", "AU-12"]
pci_dss: ["8.1.1", "8.5", "10.2.5"]
gdpr: ["32.1.b"]
mitre_attack:
  TA0003:
    T1136: ["T1136.001"]
  TA0004:
    T1078: []