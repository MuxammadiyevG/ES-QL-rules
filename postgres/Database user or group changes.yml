rule_id: 2a70f516-edc2-4faa-9389-6d124b6e2235
name_uz: "Ma'lumotlar bazasi foydalanuvchi yoki guruh o'zgarishlari"
name_ru: "Изменения пользователей или групп базы данных"
name_en: "Database user or group changes"
description_uz: |
  Ushbu qoida Persistence texnikasiga bog'liq bo'lib, `Database user or group changes` kontekstida ma'lumotlar bazasidagi foydalanuvchi yoki guruh o'zgarishlarini aniqlaydi.
  DDL changes rule dan farqli o'laroq, bu rule faqat User va Role bilan bog'liq DDL operatsiyalarini kuzatadi: CREATE USER, ALTER USER, DROP USER, CREATE ROLE, ALTER ROLE, DROP ROLE, GRANT, REVOKE.
  Bu turdagi hodisa hujumchi database ga yangi backdoor akkaunt yaratish, mavjud akkaunt huquqlarini oshirish yoki muhim foydalanuvchini o'chirish maqsadida faoliyat yuritayotganligini ko'rsatishi mumkinligi sababli xavfli hisoblanadi.
  Qonuniy holatlar sifatida rejalashtirilgan foydalanuvchi boshqaruvi, IAM jarayonlari yoki administrator tomonidan bajarilgan huquq o'zgarishlari uchrashi mumkin.
  SOC triage bosqichlari:
  - Hodisadagi `user.name`, `db.name` va `postgresql.log.sql_operation` ni aniq identifikatsiya qiling.
  - O'zgarish uchun tasdiqlangan change ticket yoki IAM yozuvi borligini tekshiring.
  - Yangi yaratilgan yoki o'zgartirilgan foydalanuvchining huquqlari (`GRANT`, `SUPERUSER`) ni tekshiring.
  - `DROP USER` yoki `REVOKE` operatsiyalari ayniqsa xavfli — muhim akkauntlar o'chirilishi mumkin.
  - Hostda boshqa shubhali hodisalar borligini tekshiring.
  - Noqonuniy faoliyat tasdiqlansa tezkor containment bajaring: yangi akkauntni o'chiring, huquqlarni qaytaring va foydalanuvchi sessiyasini to'xtating.
description_ru: |
  Данное правило связано с техникой Persistence (закрепление) и в контексте `Database user or group changes` выявляет изменения пользователей или групп в базе данных.
  В отличие от правила DDL changes, это правило отслеживает только операции DDL, связанные с User и Role: CREATE USER, ALTER USER, DROP USER, CREATE ROLE, ALTER ROLE, DROP ROLE, GRANT, REVOKE.
  Событие такого типа считается опасным, поскольку может указывать на то, что злоумышленник действует с целью создания в базе данных нового backdoor-аккаунта, повышения привилегий существующего аккаунта или удаления важного пользователя.
  В качестве легитимных случаев могут встречаться плановое управление пользователями, процессы IAM или изменения прав, выполненные администратором.
  Шаги SOC-триажа:
  - Точно идентифицируйте `user.name`, `db.name` и `postgresql.log.sql_operation` в событии.
  - Проверьте, есть ли для изменения утверждённый change ticket или запись IAM.
  - Проверьте права (`GRANT`, `SUPERUSER`) вновь созданного или изменённого пользователя.
  - Операции `DROP USER` и `REVOKE` особенно опасны — могут быть удалены важные аккаунты.
  - Проверьте, есть ли на хосте другие подозрительные события.
  - При подтверждении нелегитимной активности выполните оперативный containment: удалите новый аккаунт, верните права и завершите сессию пользователя.
description_en: |
  This rule is related to the Persistence technique and, in the context of `Database user or group changes`, detects user or group changes in the database.
  Unlike the DDL changes rule, this rule tracks only the DDL operations related to User and Role: CREATE USER, ALTER USER, DROP USER, CREATE ROLE, ALTER ROLE, DROP ROLE, GRANT, REVOKE.
  An event of this type is considered dangerous because it may indicate that an attacker is acting with the purpose of creating a new backdoor account in the database, escalating the privileges of an existing account or deleting an important user.
  Legitimate cases may include planned user management, IAM processes or privilege changes performed by an administrator.
  SOC triage steps:
  - Precisely identify `user.name`, `db.name` and `postgresql.log.sql_operation` in the event.
  - Check whether there is an approved change ticket or IAM record for the change.
  - Check the privileges (`GRANT`, `SUPERUSER`) of the newly created or modified user.
  - `DROP USER` and `REVOKE` operations are especially dangerous — important accounts may be deleted.
  - Check whether there are other suspicious events on the host.
  - If illegitimate activity is confirmed, perform rapid containment: delete the new account, revert the privileges and terminate the user session.
type: esql
params: '{}'
schedule_interval: 5m
index:
  - logs-postgresql*
query_language: esql
query: |
  FROM logs-postgresql*
  | WHERE @timestamp >= NOW() - 300 seconds
  | WHERE event.dataset == "postgresql.log"
  | WHERE event.category == "database"
  | WHERE event.action == "postgres-statement"

  // Faqat User/Role bilan bog'liq DDL operatsiyalari
  | WHERE postgresql.log.statement RLIKE "(?i).*(CREATE USER|ALTER USER|DROP USER|CREATE ROLE|ALTER ROLE|DROP ROLE|GRANT|REVOKE|CREATE GROUP|ALTER GROUP|DROP GROUP).*"

  | EVAL host_name   = host.hostname
  | EVAL actor       = user.name
  | EVAL db_name     = db.name
  | EVAL sql_op      = postgresql.log.sql_operation
  | EVAL sql_stmt    = postgresql.log.statement
  | EVAL pg_severity = postgresql.log.severity
  | EVAL pid         = process.pid

  | EVAL is_critical_op = CASE(
      postgresql.log.statement RLIKE "(?i).*DROP USER.*",        "YES - DROP USER",
      postgresql.log.statement RLIKE "(?i).*DROP ROLE.*",        "YES - DROP ROLE",
      postgresql.log.statement RLIKE "(?i).*REVOKE.*",           "YES - REVOKE",
      postgresql.log.statement RLIKE "(?i).*SUPERUSER.*",        "YES - SUPERUSER granted",
      postgresql.log.statement RLIKE "(?i).*ALL PRIVILEGES.*",   "YES - ALL PRIVILEGES",
      postgresql.log.statement RLIKE "(?i).*CREATE USER.*",      "MEDIUM - CREATE USER",
      postgresql.log.statement RLIKE "(?i).*CREATE ROLE.*",      "MEDIUM - CREATE ROLE",
      postgresql.log.statement RLIKE "(?i).*ALTER USER.*",       "MEDIUM - ALTER USER",
      postgresql.log.statement RLIKE "(?i).*GRANT.*",            "LOW - GRANT",
      "LOW"
    )

  | EVAL event_type = CASE(
      postgresql.log.statement RLIKE "(?i).*(CREATE USER|CREATE ROLE|CREATE GROUP).*",  "User/Role Created",
      postgresql.log.statement RLIKE "(?i).*(ALTER USER|ALTER ROLE|ALTER GROUP).*",     "User/Role Modified",
      postgresql.log.statement RLIKE "(?i).*(DROP USER|DROP ROLE|DROP GROUP).*",        "User/Role Deleted",
      postgresql.log.statement RLIKE "(?i).*GRANT.*",                                   "Privilege Granted",
      postgresql.log.statement RLIKE "(?i).*REVOKE.*",                                  "Privilege Revoked",
      "Other User Change"
    )

  | WHERE host_name IS NOT NULL
     OR actor IS NOT NULL
  | STATS
      event_count    = COUNT(*),
      host_names     = VALUES(host.hostname),
      actors         = VALUES(user.name),
      db_names       = VALUES(db.name),
      sql_ops        = VALUES(postgresql.log.sql_operation),
      sql_stmts      = VALUES(postgresql.log.statement),
      event_types    = VALUES(event_type),
      critical_ops   = VALUES(is_critical_op),
      pids           = VALUES(process.pid),
      last_seen      = MAX(@timestamp),
      first_seen     = MIN(@timestamp)
    BY agent_id, host.hostname, db.name, user.name, event_type
  | WHERE event_count >= 1
  | EVAL
      host_name         = host.hostname,
      db_name           = db.name,
      user              = user.name
  | KEEP
      agent_id,
      host_name,
      db_name,
      user,
      event_count,
      host_names,
      actors,
      db_names,
      sql_ops,
      sql_stmts,
      event_types,
      critical_ops,
      pids,
      last_seen,
      first_seen
  | SORT event_count DESC
enabled: true
tags:
  - attack.persistence
  - attack.t1098.001
  - attack.t1136
  - detection.threat-hunting
severity: medium
risk_score: 65
references:
  - "https://attack.mitre.org/techniques/T1098/001/"
  - "https://attack.mitre.org/techniques/T1136/"
  - "https://www.postgresql.org/docs/current/sql-createuser.html"
nist: ["AC-2", "AC-6", "AU-9", "CM-3"]
pci_dss: ["8.1.1", "8.1.2", "10.2.5"]
gdpr: ["32.1.b"]
mitre_attack:
  TA0003:
    T1098: ["T1098.001"]
    T1136: []