rule_id: 5c1ff6ba-1f76-4275-a7ba-e53de4f304b4
name_uz: "O'chirilgan / muddati o'tgan / qulflangan akkauntga kirish urinishi"
name_ru: "Попытка входа отключённой / просроченной / заблокированной учётной записи"
name_en: "Disabled / Expired / Locked Account Logon Attempt"
description_uz: |
  Ushbu qoida o'chirilgan, muddati o'tgan yoki qulflangan akkauntga kirish urinishini
  aniqlaydi (4625, SubStatus): 0xC0000072 (disabled), 0xC0000193 (expired), 0xC0000234
  (locked), 0xC0000070 (workstation restriction), 0xC0000071 (password expired). Eski/o'chirilgan
  akkauntdan foydalanishga urinish shubhali (T1078).
  SOC triage:
  - `target_users` qaysi akkaunt, `src_ips` qayerdan? Nega o'chirilgan akkaunt urinilyapti?
description_ru: |
  Это правило выявляет попытку входа в отключённую, просроченную или заблокированную
  учётную запись (4625, SubStatus): 0xC0000072 (disabled), 0xC0000193 (expired), 0xC0000234
  (locked), 0xC0000070 (workstation restriction), 0xC0000071 (password expired). Попытка использовать старую/отключённую
  учётную запись подозрительна (T1078).
  SOC triage:
  - Какая учётная запись в `target_users`, откуда `src_ips`? Почему выполняется попытка входа отключённой учётной записи?
description_en: |
  This rule detects a logon attempt to a disabled, expired or locked account
  (4625, SubStatus): 0xC0000072 (disabled), 0xC0000193 (expired), 0xC0000234
  (locked), 0xC0000070 (workstation restriction), 0xC0000071 (password expired). An attempt to use an old/disabled
  account is suspicious (T1078).
  SOC triage:
  - Which account is in `target_users`, where do `src_ips` come from? Why is a disabled account being attempted?
type: esql
params: '{}'
schedule_interval: 10m
index:
- logs-winlog*
query_language: esql
query: |
  FROM logs-winlog*
  | WHERE @timestamp >= NOW() - 15 minutes
  | WHERE winlog.channel == "Security"
  | WHERE event.code == "4625"
  | EVAL sub = TO_LOWER(COALESCE(winlog.event_data.SubStatus, "-"))
  | EVAL target_user = winlog.event_data.TargetUserName
  | EVAL src_ip = COALESCE(winlog.event_data.IpAddress, "-")
  | EVAL reason = CASE(
      sub == "0xc0000072", "Account Disabled",
      sub == "0xc0000193", "Account Expired",
      sub == "0xc0000234", "Account Locked",
      sub == "0xc0000070", "Workstation Restriction",
      sub == "0xc0000071", "Password Expired",
      sub)
  | WHERE sub IN ("0xc0000072", "0xc0000193", "0xc0000234", "0xc0000070", "0xc0000071")
  | WHERE target_user IS NOT NULL AND NOT target_user LIKE "*$"
  | STATS
      attempts = COUNT(*),
      unique_users = COUNT_DISTINCT(target_user),
      target_users = VALUES(target_user),
      src_ips = VALUES(src_ip),
      reasons = VALUES(reason),
      first_seen = MIN(@timestamp),
      last_seen = MAX(@timestamp)
    BY agent_id, winlog.computer_name, bucket = DATE_TRUNC(30 minutes, @timestamp)
  | WHERE attempts >= 1
  | EVAL dyn_risk = CASE(unique_users >= 3 OR attempts >= 5, 82, 72)
  | KEEP agent_id, bucket, winlog.computer_name, attempts, unique_users, target_users, src_ips, reasons, dyn_risk, first_seen, last_seen
  | SORT dyn_risk DESC, attempts DESC
  | LIMIT 200
enabled: true
tags:
  - attack.initial-access
  - attack.t1078
  - AuthAnomaly
severity: medium
risk_score: 75
mitre_attack:
  TA0001:
    T1078: []
nist: ["AC-7", "AU-12", "SI-4"]
pci_dss: ["10.2.4", "8.1.6"]
gdpr: ["32.1.a"]
